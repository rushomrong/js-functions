function difference(num1, num2) {
  const diff = num1 - num2;
  console.log(num1, num2, "difference is", diff);
}

const fatherAge = 44;
const motherAge = 35;

difference(fatherAge, motherAge); // call the function
